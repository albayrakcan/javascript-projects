class RandomNumber {
    generate() {
        let randomNumber;
        randomNumber = Math.floor(Math.random() * 10) + 1; // Returns a random integer from 1 to 100:
        return randomNumber;   
    }

}